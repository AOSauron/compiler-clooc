/* Niveau 2 :
  - Classes avec méthodes et sans attributs
  - Instanciation des classes
  - Appels des méthodes
*/

class Math =
(
  var ntm : int;
  method pow(a : int, b : int) : int
  {
    var retval : int;
    var i : int;

    if b == 0
    then
      return (1);
    fi

    retval := 1;
    for i in 3*5 .. 50
    do
      write "ok";
      /*retval := retval * a;*/
    end

    return (retval);
  }
)

var m : Math;
m := new Math;
write m.pow(2, 3);
