class Item = (
  method init(a : int, b : string )
  {
    current := 1;
    previous := 0;
    generation := 1;
  }

  method nextGen(x : int, y : int)
  {
    var temp : int;
    temp := current;
    current := current + previous;
    previous := temp;
    generation := generation + 1;
  }
)

var item : Item;
item := new Fibonacci;

do item.init(13).nextGen(23,67);

do item.init(15, "initialiser");
